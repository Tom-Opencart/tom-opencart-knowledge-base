# Modules

Module knowledge packs are created only when a real task touches a specific module.

Each module note should capture:

- controllers, models, views, languages
- install/uninstall logic
- XML modifiers
- SQL/table creation
- events
- related domains in core OpenCart
