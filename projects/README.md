# Projects

Project overlays are created only when a real store task requires them.

Each project overlay should capture:

- active theme
- active module stack
- custom tables
- custom XML modifiers
- real conflict zones
- deviations from baseline core behavior
