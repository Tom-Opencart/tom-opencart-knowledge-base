# Themes

Theme knowledge packs are created only when a real task touches a specific theme.

Each theme note should capture:

- overridden templates
- theme controllers/models
- bundled modules
- XML modifiers
- product/cart/checkout/mail override risks
- known collision hotspots
